# Example: Simulating a mock `QuantumProgram`

```{warning}
This chapter is currently a plan/outline, and may not represent the exact implementation once completed. 
```

The documentation of this will basically be to call an `simulate()` function or something, but I want this page to go step-by-step through the execution of a dummy `QuantumProgram`, i.e. we won't do an actual simulation but define some dummy `Record`/`Instruction`/`QECCode` to showcase how it could work.
This will both demonstrate the flexibility and serve as a test for the whole workflow without getting bogged down in true simulation/interface issues.